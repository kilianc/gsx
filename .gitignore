# Build output
/bin/
/dist/
*.vsix

# Rendered documentation site (built by CI, deployed to Pages)
/docs/dist/

# Editor extension build artifacts
vscode/gsx-vscode/node_modules/
vscode/gsx-vscode/dist/

# Playground editor bundle sources. The built output in docs/static/vendor is
# checked in on purpose, so building the site needs only Go.
web/node_modules/
vscode/gsx-vscode/*.vsix

# Local scratch
.tmp/
issues/
.DS_Store

# Local tooling: editor launch config, and the worktrees this repo is checked
# out into. Committing it would nest the repo inside itself.
.claude/
